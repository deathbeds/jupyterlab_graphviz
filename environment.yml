# This is used by repo2docker. conda can install all the Jupyter dependencies.
name: jupyterlab-graphviz-dev

channels:
  - conda-forge
  - defaults

dependencies:
  - jupyterlab >=0.35,<0.36
  - nodejs >=8.9.12,<9
  - python >=3.7,<3.8
